import { Router } from 'express';

export const projectsRoutes = Router();

// TODO: Implement project creation logic in projects.controller.ts → projects.service.ts
// projectsRoutes.post('/', authMiddleware, validate(createProjectSchema), createProject);

// TODO: Implement project listing logic in projects.controller.ts → projects.service.ts
// projectsRoutes.get('/', authMiddleware, listProjects);